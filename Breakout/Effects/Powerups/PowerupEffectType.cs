namespace Breakout.Effects.Powerups;

/// <summary>
/// This Enum contains an entry for each powerup effect. If a new one is be implemented, it should
/// be added to this enum.
/// </summary>
public enum PowerupEffectType {
    ExtraLife,
    Wide,
    DoubleSize,
    Split,
    HardBall
}
